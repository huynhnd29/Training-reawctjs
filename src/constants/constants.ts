enum STATUS {
  ACTIVE = 1,
  INACTIVE = 0,
}

enum EKeyCode {
  ENTER = 'Enter',
  NUMPAD_ENTER = 'NumpadEnter',
}
enum EKeyCodeNumber {
  ENTER = 13,
}

export {
  STATUS,
  EKeyCode,
  EKeyCodeNumber,
};
